- input to llm is input token
- output from llm is output token

- we are actually talking to gpt
- GPT is generative pretrained transformer
- Generative is it generates output for you
- Pre trained in nature that is it was trained on data that is already available
