##### mutex lock prevents race condition

- If two threads try to access and modify the same thing
- Mutex lock prevents race condition

### coroutines

- special functions that can be paused
- await pauses execution until result is ready
- asyncio build in python library
- event loop the engine that runs and schedule co-routines in python
